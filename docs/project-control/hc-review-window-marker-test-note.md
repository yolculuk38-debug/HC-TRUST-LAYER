# HC Review Window Marker Test Note

This temporary docs-only note exists to trigger the HC Review Window Marker workflow after PR #1115.

It does not change HC:// protocol behavior, validation behavior, records, schemas, workflows, policy, generated artifacts, or canonical artifacts.

advisory_only=true
public_safe=true
truth_guarantee=false
